# Backend

Node.js + Express API for Crypto Guardian.

## Setup

```bash
cp .env.example .env
pnpm install
pnpm dev
```

Access at: http://localhost:3001

## Environment Variables

- `PORT` — Server port (default: 3001)
- `ETHERSCAN_API_KEY` — Free key from https://etherscan.io/apis
